# Role: 高级 Python 系统架构师
# Task: 修复 TypeError 并重构扫描交互逻辑

## 一、 紧急 Bug 修复 (main.py)
1. 修复 `run_scanner_with_progress` 调用时的 `TypeError`。确保 `scan_func` 仅通过关键字参数或位置参数传递一次，检查函数定义与调用的匹配情况。
2. 捕获扫描过程中的所有异常，确保即使单个目录权限报错，程序也能通过 `rich.print` 提示“部分路径跳过”而非崩溃。

## 二、 功能合并与交互降维
1. **合并功能**：将菜单中的“自动扫描推荐文件”与“推荐压缩当前目录”合并为一个选项：【🚀 智能扫描并建议压缩】。
   - 逻辑：扫描指定目录及其子目录，匹配 builtin_rules.json，输出可压缩清单。
2. **修正文件夹选择**：
   - 所有的自定义选择必须使用 `tkinter.filedialog.askdirectory()` (选文件夹)，严禁使用 `askopenfilename` (选文件)。
   - 在 UI 提示语中增加解释：“[说明] 请选择一个文件夹，助手将自动分析该目录及其子目录下所有的安装包与临时文件。”

## 三、 极限释放 (Extreme Release) 逻辑透明化
1. 在进入“极限释放”菜单前，增加一段大白话说明面板：
   - “🚨 什么是极限释放？这是最后一步。当您的文件已安全移入隔离箱并完成 ZIP 备份后，此操作将彻底清理隔离箱副本以释放物理空间。”
2. 确保极限释放的触发必须满足：`state == 'archived'` 且经过用户手动确认。

## 四、 代码质量要求
- 保持 `state_file.json` 的原子写入逻辑。
- 所有 UI 文案必须平易近人，去除“扫描函数”、“参数”等技术词汇。